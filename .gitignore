
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node modules
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# Environment Variables
.env
.env.*

# Supabase CLI local cache (project refs, version pins - not meant to be shared)
supabase/.temp

!.env.example

# Reference-only database snapshot - a full dump of live schema/policies for your
# own records, not part of the app itself. Keep it locally, don't commit it.
master_schema.sql
